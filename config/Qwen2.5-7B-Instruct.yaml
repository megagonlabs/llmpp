model_args:
  device_map: "auto"
  pretrained_model_name_or_path: "models/Qwen2.5-7B-Instruct"
  dtype: "bfloat16"

tokenizer_args:
  pretrained_model_name_or_path: null  # defaults to model.model_name_or_path
  pad_token: "<|endoftext|>"  # see https://github.com/QwenLM/Qwen/blob/main/tokenization_note_ja.md#%E7%89%B9%E5%88%A5%E3%81%AA%E3%83%88%E3%83%BC%E3%82%AF%E3%83%B3

collator_args:
  response_template: "<|im_start|>assistant\n"
  instruction_template: "<|im_start|>user\n"
  mlm: false

dataset_args:
  train_jsonl_path: "data/en_ewt-r2.15/en-turn1-step3.train.jsonl"
  replace_system_role: false

sft_config_args:
  output_dir: null  # defaults to {model.model_name_or_path}_trl[-lora]_{dataset.train_jsonl_path#stem}
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8
  gradient_checkpointing: true
  num_train_epochs: 3
  learning_rate: 3.0e-4
  save_strategy: "no"
  logging_steps: 0.01
  # eval_steps: 0.25
  eval_strategy:  "no"
  lr_scheduler_type: "cosine_with_min_lr"
  lr_scheduler_kwargs:
    min_lr_rate: 0.1
  bf16: true
  max_seq_length: 8192

lora_args:
  use_lora: true
  target_modules: "all-linear"
  r: 8
  lora_dropout: 0.05
  task_type: "CAUSAL_LM"

chat_template_replace_pattern: null
